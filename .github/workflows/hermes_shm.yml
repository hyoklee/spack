name: hermes_shm

on:
  push:
    branches: [ develop- ]
  pull_request:
    branches: [ develop- ]
  workflow_dispatch:
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout spack
      uses: actions/checkout@v4
    - name: spack install hermes_shm
      run: |
        . ./share/spack/setup-env.sh
        spack compiler find
        spack external find
        spack spec hermes_shm
        spack install hermes_shm
